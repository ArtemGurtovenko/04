function taskmain() {
    const d = prompt("Enter money")
    if (d<=0) {
        alert('Invalid value')
    }
    else {
        alert(d/100*5/12*2)
        Math.round(d/100*5/12*2)
    }
}
function tasknormal() {
    const d = promt("Enter stroke")

}

// Я якщо честно так і не зміг зрозуміти як зробити перегортання тексту. Я знаходив відповідь в інтернеті але намагався зробити
// як ви показували на уроці, але не виходить.